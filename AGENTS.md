# Bragi Canvas skill maintenance

Keep the skill host-neutral and English. When plugin MCP fields or task semantics change, update SKILL.md and the tools, gotchas and workflows references together. State the paired plugin version. Use a matching branch suffix and link paired PRs. Run `npm run verify` before merging; the workspace sync check supports explicit plugin and skill directories.
